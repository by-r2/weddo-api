$kind: http-request
description: Categorias distintas já usadas nos presentes do catálogo (select no
  admin ou filtro público).
url: "{{baseUrl}}/w/{{weddingId}}/gift-categories"
method: GET
order: 1000
