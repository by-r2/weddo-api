$kind: http-request
description: Retorna detalhes do convite com lista de convidados.
url: "{{baseUrl}}/admin/invitations/{{invitationId}}"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('Status 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 3000
