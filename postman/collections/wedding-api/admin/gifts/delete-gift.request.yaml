$kind: http-request
url: "{{baseUrl}}/admin/gifts/{{giftId}}"
method: DELETE
scripts:
  - type: afterResponse
    code: pm.test('Status 204', () => pm.response.to.have.status(204));
    language: text/javascript
order: 6000
